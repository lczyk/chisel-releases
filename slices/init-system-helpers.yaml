package: init-system-helpers

essential:
  init-system-helpers_copyright:

slices:
  # bins:
  #   contents:
  #     /usr/bin/deb-systemd-helper:
  #     /usr/bin/deb-systemd-invoke:
  #     /usr/sbin/invoke-rc.d:
  #     /usr/sbin/service:
  #     /usr/sbin/update-rc.d:

  scripts:
    essential:
      init-system-helpers_deb-systemd-helper:
      init-system-helpers_deb-systemd-invoke:

  deb-systemd-helper:
    essential:
      perl-base_bins:
      perl-base_modules:
    contents:
      /usr/bin/deb-systemd-helper:

  deb-systemd-invoke:
    essential:
      base-files_bin:
      dash_bins:
      perl-base_bins:
      perl-base_modules:
      systemd_bins:
    contents:
      /usr/bin/deb-systemd-invoke:

  invoke-rc-d:
    essential:
      base-files_bin:
      dash_bins:
      sed_bins:
      findutils_xargs:
      procps_ps-bin:
      coreutils_ls-utility:
      coreutils_cat:
      coreutils_basename:
      grep_bins:
    contents:
      /usr/sbin/invoke-rc.d:

  config:
    contents:
      /etc/rc0.d/:
      /etc/rc1.d/:
      /etc/rc2.d/:
      /etc/rc3.d/:
      /etc/rc4.d/:
      /etc/rc5.d/:
      /etc/rc6.d/:
      /etc/rcS.d/:
      /etc/systemd/system/:
      /etc/systemd/user/:

  var:
    contents:
      /var/lib/systemd/:

  copyright:
    contents:
      /usr/share/doc/init-system-helpers/copyright:
